<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <%- include('partials/nav') %>
    <div class="profileInfo">
      <p>Username: <%=user.username %></p>
      <p>Email: <%=user.email %> </p>
    </div>
      <div>
        <form class="ejs-form" action="/createPost" enctype="multipart/form-data" method="POST">
          <div>
          <input name="title" placeholder="Post Title">
          <input name="caption" placeholder="Post Caption">
          <button type="submit" value = "upload">Submit</button>
          </div>
            <div class="imgUpload">
              <label for="imgUpload">Image</label>
              <input type="file" id="imgUpload" name="file">
            </div>
        </form>
      </div>   
      <div>
        <a href="/feed" class="viewPosts">View All Posts</a>
      </div>
      <div class="yourPosts">
        Your posts are below 
      </div>
    <div class ="profile-feed">
      <ul>
          <% for(let i=0; i<posts.length; i++) { %>
          <li> Title : <%= posts[i].title %> </li>
          <li> Caption : <%= posts[i].caption %> </li>
          <img class="profileImg" src="<%=posts[i].image%>">
          <% } %>
      </ul>
    </div>

</body>
</html>